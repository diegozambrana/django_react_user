import React from 'react';
import { Button } from 'reactstrap';

const Login = (props) => {

  return (
      <div>
        <p>Login Component!!!!</p>
        <Button>Login</Button>
      </div>
  )
}

export default Login;